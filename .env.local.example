NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Figma REST API — tooling only, read by scripts/figma.sh and the design agents.
# NOT read by the Next.js app: never prefix this NEXT_PUBLIC_, or it ships in the
# client bundle. Create at figma.com → Settings → Security → Personal access tokens.
# Required scopes: file_content:read, file_dev_resources:read.
# See docs/figma-api.md for scopes, endpoints and plan limits.
FIGMA_ACCESS_TOKEN=figd_your-token-here
FIGMA_FILE_KEY=gDoteM1ow1AZpSEGSNhpc7
